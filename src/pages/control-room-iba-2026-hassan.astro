---
const repo = "Hassan-Alam-Cloud/capstone-program-site";

// GitHub edit links (direct to folders)
const githubMentors =
  `https://github.com/${repo}/tree/main/src/content/mentors`;

const githubUpdates =
  `https://github.com/${repo}/tree/main/src/content/updates`;

const githubUploads =
  `https://github.com/${repo}/tree/main/public/images/uploads`;

const githubRepoHome =
  `https://github.com/${repo}`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Capstone Control Room</title>

    <style>
      :root {
        --bg: #070a12;
        --card: rgba(255, 255, 255, 0.06);
        --card2: rgba(255, 255, 255, 0.04);
        --stroke: rgba(255, 255, 255, 0.12);
        --text: rgba(255, 255, 255, 0.92);
        --muted: rgba(255, 255, 255, 0.70);
        --muted2: rgba(255, 255, 255, 0.55);
        --accent: #7c5cff;
        --accent2: #2fd6c3;
        --danger: #ff4d6d;
        --shadow: 0 18px 50px rgba(0, 0, 0, 0.45);
        --radius: 18px;
      }

      * { box-sizing: border-box; }
      html, body { height: 100%; }

      body {
        margin: 0;
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto,
          Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
        background: radial-gradient(1200px 600px at 15% 10%, rgba(124, 92, 255, 0.22), transparent 60%),
          radial-gradient(1200px 600px at 90% 25%, rgba(47, 214, 195, 0.16), transparent 60%),
          radial-gradient(800px 500px at 50% 85%, rgba(255, 77, 109, 0.10), transparent 70%),
          var(--bg);
        color: var(--text);
      }

      .wrap {
        max-width: 1100px;
        margin: 0 auto;
        padding: 36px 18px 80px;
      }

      .top {
        display: flex;
        align-items: flex-start;
        justify-content: space-between;
        gap: 18px;
        flex-wrap: wrap;
        margin-bottom: 22px;
      }

      .brand {
        display: flex;
        flex-direction: column;
        gap: 8px;
      }

      .brand h1 {
        margin: 0;
        font-size: 28px;
        letter-spacing: -0.02em;
      }

      .brand p {
        margin: 0;
        max-width: 680px;
        color: var(--muted);
        line-height: 1.55;
        font-size: 14px;
      }

      .pill {
        display: inline-flex;
        align-items: center;
        gap: 8px;
        padding: 10px 12px;
        border: 1px solid var(--stroke);
        border-radius: 999px;
        background: rgba(255,255,255,0.04);
        color: var(--muted);
        font-size: 13px;
      }

      .grid {
        display: grid;
        grid-template-columns: repeat(12, 1fr);
        gap: 16px;
      }

      .card {
        border: 1px solid var(--stroke);
        border-radius: var(--radius);
        background: linear-gradient(180deg, var(--card), var(--card2));
        box-shadow: var(--shadow);
        padding: 18px;
      }

      .card h2 {
        margin: 0 0 8px 0;
        font-size: 16px;
        letter-spacing: -0.01em;
      }

      .card p {
        margin: 0 0 14px 0;
        color: var(--muted);
        line-height: 1.55;
        font-size: 13px;
      }

      .col-7 { grid-column: span 7; }
      .col-5 { grid-column: span 5; }
      .col-12 { grid-column: span 12; }

      @media (max-width: 900px) {
        .col-7, .col-5 { grid-column: span 12; }
      }

      .btns {
        display: flex;
        flex-wrap: wrap;
        gap: 10px;
      }

      a.btn {
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
        padding: 12px 14px;
        border-radius: 14px;
        border: 1px solid var(--stroke);
        text-decoration: none;
        color: var(--text);
        font-size: 14px;
        background: rgba(255,255,255,0.05);
        transition: transform 120ms ease, border 120ms ease, background 120ms ease;
      }

      a.btn:hover {
        transform: translateY(-1px);
        border-color: rgba(255,255,255,0.22);
        background: rgba(255,255,255,0.08);
      }

      a.btn.primary {
        border-color: rgba(124, 92, 255, 0.55);
        background: rgba(124, 92, 255, 0.12);
      }

      a.btn.primary:hover {
        background: rgba(124, 92, 255, 0.18);
        border-color: rgba(124, 92, 255, 0.75);
      }

      a.btn.good {
        border-color: rgba(47, 214, 195, 0.55);
        background: rgba(47, 214, 195, 0.10);
      }

      a.btn.good:hover {
        background: rgba(47, 214, 195, 0.15);
        border-color: rgba(47, 214, 195, 0.75);
      }

      a.btn.danger {
        border-color: rgba(255, 77, 109, 0.55);
        background: rgba(255, 77, 109, 0.10);
      }

      a.btn.danger:hover {
        background: rgba(255, 77, 109, 0.14);
        border-color: rgba(255, 77, 109, 0.75);
      }

      .small {
        font-size: 12px;
        color: var(--muted2);
        line-height: 1.6;
      }

      ol {
        margin: 0;
        padding-left: 18px;
        color: var(--muted);
        font-size: 13px;
        line-height: 1.7;
      }

      code {
        font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas,
          "Liberation Mono", "Courier New", monospace;
        font-size: 12px;
        color: rgba(255,255,255,0.90);
        background: rgba(255,255,255,0.08);
        padding: 2px 6px;
        border-radius: 10px;
        border: 1px solid rgba(255,255,255,0.12);
      }

      .footer {
        margin-top: 18px;
        color: var(--muted2);
        font-size: 12px;
      }
    </style>
  </head>

  <body>
    <div class="wrap">
      <div class="top">
        <div class="brand">
          <h1>Capstone Control Room</h1>
          <p>
            Private admin dashboard for The Capstone Program.
            This is protected by Cloudflare Access — only you should reach this page.
          </p>
        </div>

        <div class="pill">
          ✅ Status: Live • Access Protected
        </div>
      </div>

      <div class="grid">
        <section class="card col-7">
          <h2>Quick Actions (No Coding)</h2>
          <p>These open the exact folders you need in GitHub. Just edit files and commit.</p>
          <div class="btns">
            <a class="btn primary" href={githubMentors} target="_blank" rel="noreferrer">
              👤 Mentors content
            </a>
            <a class="btn primary" href={githubUpdates} target="_blank" rel="noreferrer">
              📢 Updates content
            </a>
            <a class="btn good" href={githubUploads} target="_blank" rel="noreferrer">
              🖼 Upload images
            </a>
            <a class="btn" href={githubRepoHome} target="_blank" rel="noreferrer">
              🧠 Open repo
            </a>
          </div>

          <p class="footer">
            Tip: uploads go in <code>public/images/uploads</code>
          </p>
        </section>

        <section class="card col-5">
          <h2>Live Site Links</h2>
          <p>Use these to verify everything is showing correctly.</p>
          <div class="btns">
            <a class="btn" href="https://the-capstone-program.pages.dev" target="_blank" rel="noreferrer">
              🌐 Main Website
            </a>
            <a class="btn" href="https://the-capstone-program.pages.dev/mentors" target="_blank" rel="noreferrer">
              👥 Mentors Page
            </a>
            <a class="btn" href="https://the-capstone-program.pages.dev/updates" target="_blank" rel="noreferrer">
              📰 Updates Page
            </a>
            <a class="btn danger" href="https://the-capstone-program.pages.dev/apply" target="_blank" rel="noreferrer">
              🧾 Apply Page
            </a>
          </div>
          <p class="small" style="margin-top: 12px;">
            If updates do not show immediately, wait 30–90 seconds for Cloudflare deploy.
          </p>
        </section>

        <section class="card col-12">
          <h2>How to Update (Fast Workflow)</h2>
          <p>This is the workflow you’ll use forever.</p>

          <ol>
            <li>Open <b>Mentors content</b> or <b>Updates content</b>.</li>
            <li>Create or edit a markdown file (example: <code>saad-bhai.md</code>).</li>
            <li>Click <b>Commit changes</b> in GitHub.</li>
            <li>Wait for Cloudflare deploy (usually 30 seconds).</li>
            <li>Refresh your live pages.</li>
          </ol>

          <p class="small" style="margin-top: 12px;">
            ✅ This avoids OAuth, avoids Decap loops, avoids rate limits, and stays fully free.
          </p>
        </section>
      </div>
    </div>
  </body>
</html>